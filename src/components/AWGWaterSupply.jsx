import React,{useState}from'react';import{Wind,Snowflake,Filter,Droplets}from'lucide-react';export default function AWGWaterSupply(){const[s,set]=useState(0);const steps=[['Air intake',Wind,'Ambient air passes through particulate and HEPA filtration.'],['Condensation',Snowflake,'Community solar and kinetic energy power cooling coils, bringing humid air below its dew point.'],['Purification',Filter,'Collected water passes through carbon filtration, reverse osmosis, and UV-C treatment in the proposed system.'],['Mineralization',Droplets,'Trace minerals are added before dispensing. A configured meter and contract can record verified liters.']];return <section className="panel"><span className="eyebrow">FROM AIR TO SHARED WATER</span><h2>A cycle worth understanding.</h2><div className="steps">{steps.map(([name,Icon],i)=><button key={name} className={s===i?'active':''} onClick={()=>set(i)}><Icon/><small>0{i+1}</small>{name}</button>)}</div><p>{steps[s][2]}</p><small className="muted">System concept. Output depends on humidity, temperature, power, and equipment; no live water-quality sensors are connected.</small></section>}
