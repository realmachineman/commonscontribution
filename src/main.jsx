import React from'react';import{createRoot}from'react-dom/client';import{BrowserRouter}from'react-router-dom';import App from'./App';import './style.css';import './glass.css';import {initializeAuth} from './supabase';const WalletProvider=React.lazy(()=>import('./Wallet'));const app=<BrowserRouter><App/></BrowserRouter>;initializeAuth().then(()=>createRoot(document.getElementById('root')).render(import.meta.env.VITE_DYNAMIC_ENVIRONMENT_ID?<React.Suspense fallback={<p>Opening your Commons…</p>}><WalletProvider>{app}</WalletProvider></React.Suspense>:app));
if('serviceWorker'in navigator)navigator.serviceWorker.getRegistrations().then(r=>r.forEach(x=>x.unregister())).then(()=>caches.keys().then(k=>Promise.all(k.map(n=>caches.delete(n))))).catch(()=>{});
